# Kern (leicht, sofort einsatzbereit) — Kurse, Suche, Portfolio, KI, Thesis
fastapi>=0.110
uvicorn[standard]>=0.29
requests
tzdata  # Börsen-Handelszeit (zoneinfo) — falls das OS keine Zeitzonendaten mitbringt

# Quant-Analyse (technische Indikatoren + Faktor-Scorecard)
numpy
pandas
ta>=0.11

# Alt: A-Aktien-Datenquellen (Eastmoney/akshare) — nur nötig für den Legacy-A-Aktien-Pfad
# akshare>=1.10
# mootdx>=0.10
